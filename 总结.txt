1.拿到这个项目，我先分析了用户需求，然后画出了表与表之间的关系图
2.创建数据库以及建表
3.根据数据表一一对应模板类，models;
4.写sdb，其中包含了数据库连接类，sql语句类根据模板类进行字符串拼接，以及专门操作表的类。
5.写路由，根据不同的表写不同的路由;
6.将数据库中的数据查询出来进行展示;

写前端
    1.由于后台sql类功能不完善，导致单个修改数据会出错，于是我就将sql的模板类换成了前端传来的对象对应进行sql语句的修改，
        A)当属性的值为undefined的时候sql类不会为其生成对应的SQL语句，这样就大大减轻了我后面的工作
    2.关于写根据条件筛选题目，我考虑到以下几点。
        a) 为了减轻数据库压力，我只查询了题目数据
        b) 由于我善于使用数组API，我拿到数据都会将其转化为数组
        c) 所以我使用了Es6中Object.values()将题目的外键slice到数组arr中.
        d) 然后使用arr数组的every方法判断页面上的attr属性所提取的id与arr对比
        e) 根据every返回值展示题目
    3.